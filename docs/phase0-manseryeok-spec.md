# Phase 0 개발 스펙 — 만세력 엔진

> 사전조사 결론은 `docs/saju-service-research.md` 참고.
> 이 문서는 **새 세션에서 조사 없이 바로 코딩을 시작하기 위한** 스펙이다.

---

## 진행 상황 (2026-08-29)

**완료**
- `packages/manseryeok/` — 만세력 엔진. 절기는 VSOP87 급수 계산, 공표값과 1분 이내. 검증 45.
- `packages/saju-rules/` — 십신·강약·용신·합충·신살, 대운·세운·일진, 궁합, 사주→공통척도, 교차검증. 검증 84.
- `packages/traits/` — 사주·관상·손금이 공유하는 8축 척도.
- `packages/physiognomy/` — 관상 십이궁 → 8축. 외모 점수화 없음.
- `packages/palmistry/` — 손금 4선·오행 손모양 → 8축. 수명 예측 없음.
- `packages/report/` — 룰 결과 → 서술형 리포트. 판단 금지·문장화만. 검증 45(모델 호출 없음).
- `packages/commerce/` — 주문·이용권·환불 정책·포트원 V2 어댑터. 검증 49(네트워크 없음).
- `packages/korean/` — 조사 처리 등 한국어 문장 유틸.
- `apps/api/` — 결제·리포트 API 서버. 검증 34(실제 결제 0건).

**검증 합계 275.** 각 패키지에서 `node --experimental-strip-types verify.ts`
- `apps/manse-viewer/` — 3탭(사주 / 궁합 / 관상·손금·교차검증). `node apps/manse-viewer/build.mjs`
- **3종 교차검증 완성.** 사주·관상·손금을 8축으로 대조해 일치/엇갈림/단독으로 분류.

**수익 경계선 확정** — `docs/saju-service-research.md` 10절 참고.
무료 = 단독 풀이 전부(원가 0). 유료 = 교차검증 + AI 서술형 리포트.

### 리포트 품질 확인 완료 (2026-08-29)

실제로 한 건 뽑아 읽어봤고, **팔 만한 품질로 확인됐다.** 검사 항목 5개 전부 통과:

- 데이터 밖 내용을 지어내지 않음 — 모든 수치가 룰 엔진 출력으로 추적됨
- 약한 부분을 먼저 말함 — "재성이 하나도 없다"를 첫 문단에서 꺼냄
- 유파를 밝힘 — "억부용신, 즉 일간의 강약을 기준으로 삼는 관점"
- 추상적 칭찬 대신 구체적 상황으로 씀
- 실행 제안으로 끝남

겁주기 금지도 실제로 지켜졌다. 재성 결핍을 다루며 "돈을 못 번다는 뜻이 아니라"를 먼저 붙였다.

실측: **원가 147원 · 51초 · 입력 6,282 / 출력 2,951 토큰.** 판매가 19,900원 대비 0.74%.
재요청은 캐시 적중으로 0원, 문장 동일.

### 다음 작업 — 결제창 붙이기

서버는 완성됐다. 브라우저 쪽만 남았다.

**선행 조건: 포트원 가입.** 결제창을 띄우려면 상점 아이디가 필요하고,
그건 사업자등록번호가 있어야 발급된다. 이건 코드로 해결되지 않는 부분이다.

새 세션 프롬프트:

```
apps/api 서버는 완성돼 있다 (verify.ts 34개 통과).
apps/manse-viewer 의 유료 패널을 실제 결제 흐름으로 바꿔줘.
/api/preview 로 미리보기를 받아 보여주고, 청약철회 고지에 동의를 받은 뒤
/api/orders 로 주문을 만들고, 포트원 브라우저 SDK로 결제창을 띄우고,
/api/orders/:id/confirm 으로 확정하는 흐름.
포트원 상점 아이디가 아직 없으면 테스트 모드로 먼저 붙여둘 것.
무료 구간은 지금처럼 브라우저에서만 돌게 그대로 둘 것.
```

그 다음: 배포 + Postgres, 사진 자동 추출, 신년 시즌 패키지(11월 마감).

### 남은 숙제

- **사진 → 특징 자동 추출 어댑터.** 룰 엔진은 완성돼 있고 입력 어댑터만 없다.
  브라우저 내 랜드마크 추출(원본 미전송)로 붙일 것. 아티팩트 미리보기 환경은
  외부 모델 로딩이 막혀 있어 현재는 수동 선택으로 데모한다.
- 서머타임 시행 첫날·마지막날의 경계 시각 미반영 (`src/timezone.ts` 주석 참고).
- 타 서비스와의 명식 100건 외부 교차검증 미실시.
- 대운수 반올림/버림은 유파차 — 현재 반올림 고정, 옵션화 가능.
- 리포트 캐시가 메모리라 프로세스가 죽으면 사라진다. 배포 전 Postgres로 옮길 것
  (`packages/report/src/cache.ts` 의 `ReportCache` 인터페이스 구현체만 갈아끼우면 된다).
- 리포트 계층은 서버 전용이다. 브라우저 페이지에서는 못 쓴다 (API 키 노출).
- 주문 저장소도 메모리다 (`apps/api/src/server.ts` 의 `MemoryOrderStore`). 배포 전 교체 필수.
- 환불 정책은 법령을 읽고 구현한 것이지 법률 자문이 아니다. 오픈 전 검토받을 것.
- 영업일 계산에 공휴일이 빠져 있다 (`packages/commerce/src/refund.ts`). 실제로는 기한이 더 늘어나므로
  이 값보다 빨리 환급하면 안전한 쪽이다.
- 포트원 결제창(브라우저 SDK) 연동은 아직 없다. 서버 쪽 검증만 완성.

---

## 왜 이것부터인가

명식(사주팔자 여덟 글자)이 틀리면 해석·관상 교차검증이 전부 무의미하다.
그리고 이 부분이 혼자 만들기 가장 어렵다. UI와 결제는 나중에 붙여도 된다.

---

## 만들 것

`packages/manseryeok/` — 의존성 없는 순수 TypeScript 모듈.

```ts
type YajaMode = 'yaja' | 'joja' | 'midnight';  // 야자시 / 조자시 / 현대식 00시 기준

interface BirthInput {
  date: string;           // 'YYYY-MM-DD' (양력)
  time: string;           // 'HH:mm', 시간 모를 경우 null 허용
  longitude?: number;     // 기본 126.978 (서울)
  yajaMode?: YajaMode;    // 기본 'midnight'
  applyTrueSolarTime?: boolean;  // 기본 true
}

interface Pillar { stem: string; branch: string; }   // 천간/지지

interface Myeongsik {
  year: Pillar; month: Pillar; day: Pillar; hour: Pillar | null;
  meta: {
    correctedTime: string;      // 보정 후 시각
    solarTimeOffsetMin: number; // 진태양시 보정량 (분)
    dstApplied: boolean;        // 서머타임 적용 여부
    monthTermName: string;      // 적용된 절기명 (예: '입춘')
    monthTermEnteredAt: string; // 그 절기의 절입시각 (분 단위)
  };
}

function calculate(input: BirthInput): Myeongsik;
```

**핵심: `meta`를 반드시 반환할 것.** 계산 근거를 화면에 공개하는 것이 우리 차별점이다.
(경쟁 서비스들이 앱마다 명식이 달라 사용자 불만을 사고 있음)

---

## 구현 규칙 — 여기서 정확도가 갈린다

### 1. 절기 (가장 중요)
- **월주는 달력 월이 아니라 절기로 정한다.** 입춘부터 새 해, 각 절(節)부터 새 달.
- 24절기 중 월의 경계가 되는 것은 12개의 **절(節)**: 입춘·경칩·청명·입하·망종·소서·입추·백로·한로·입동·대설·소한
- 절입시각은 **분 단위**로 계산. 태양 황경이 315°(입춘), 345°(경칩), 15°(청명)… 가 되는 순간.
- **테이블로 박지 말 것.** VSOP87 축약판이나 Meeus 알고리즘으로 계산. 데이터가 짧아지고 정확도는 더 좋다.

### 2. 진태양시
- 표준시(UTC+9)는 동경 135° 기준. 서울(126.978°)은 약 **−32분** 보정.
- 공식: `(경도 − 135) × 4분`
- 균시차(equation of time)까지 반영할지는 옵션으로. 기본은 경도 보정만.

### 3. 서머타임
한국 시행 구간 (이 기간 출생자는 **−1시간**):
```
1948~1951, 1955~1960, 1987~1988
```
정확한 시작·종료 일시는 구현 시 확인해서 테이블로 둘 것. (이건 짧으니 테이블 OK)

### 4. 야자시 / 조자시
- `midnight` (기본): 00:00을 날짜 경계로. 23:00~23:59는 당일 일주, 자시.
- `yaja`: 23:00 이후는 **다음날 일주**로 넘김.
- `joja`: 23:00~23:59를 당일 일주 + 자시로 별도 표기.
- **정답이 없는 영역이므로 사용자가 고를 수 있게 하고, 기본값을 화면에 명시한다.**

### 5. 일주
- 60갑자 순환. 기준일을 하나 정하고 율리우스일수로 계산.
- **기준일 하나만 틀려도 모든 사용자의 명식이 어긋난다.** 검증 최우선 대상.

---

## 검증 스크립트 (엔진과 같이 만들 것)

`packages/manseryeok/verify.ts`

1. **경계 케이스 테스트** — 이것들이 통과해야 함:
   - 입춘 당일, 절입시각 직전 1분 / 직후 1분 → 년주가 바뀌어야 함
   - 각 절의 절입 경계 → 월주가 바뀌어야 함
   - 23:00 / 23:59 / 00:00 → yajaMode별로 결과가 달라야 함
   - 서머타임 시행 연도의 여름 출생
   - 윤년 2월 29일

2. **외부 대조** — 타 서비스 3곳과 100건 교차검증.
   불일치가 나오면 **원인을 설명할 수 있어야** 한다 (야자시 해석 차이 등).
   설명 못 하는 불일치 = 버그.

---

## 하지 말 것

- ❌ LLM에게 간지 계산 시키기 (패턴 추측이라 틀린다)
- ❌ 절기 데이터를 외부 API로 가져오기 (남의 가용성에 정확도가 묶인다)
- ❌ 이 단계에서 UI·결제·해석문 만들기

---

## 이 다음 순서

1. Phase 0: 엔진 + 검증 ← **여기**
2. 명식 뷰어 UI (무료 공개, 검색 유입 씨앗)
3. 해석 룰 엔진 (십신·오행·용신) → LLM은 문장화만
4. 관상 (MediaPipe FaceLandmarker, 브라우저 내 처리)
5. 손금 → 3종 교차검증 리포트
6. 궁합 + 신년 시즌 패키지 (11월 오픈이 마감선)
