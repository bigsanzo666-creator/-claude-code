# 배포 — neulbomsaju.co.kr

> 목표는 하나다. **포트원 심사자가 주소를 열었을 때 사이트가 떠 있는 것.**
> 결제가 돌아갈 필요는 없다. 심사가 보는 건 사이트의 존재와 정책 페이지다.

## 완료 (2026-08-30)

**https://neulbomsaju.co.kr 로 떠 있다.** 심사 대기 모드 — 결제·리포트는 꺼져 있고
무료 풀이와 정책 페이지는 동작한다.

| | |
|---|---|
| 서버 | 렌더 싱가포르, `0.5c-512mb` (월 $7) |
| 임시 주소 | `neulbomsaju.onrender.com` (그대로 살려둠) |
| DNS | 가비아. `@` → A `216.24.57.1` · `www` → CNAME |
| HTTPS | 렌더 자동 발급 완료 |
| 확인한 경로 | `/` `/terms` `/privacy` `/refund` `/healthz` 전부 200 |

도커 이미지 빌드는 첫 시도에 통과했다 (미검증이라고 적어뒀던 부분).

청사진이 한 번 거부됐던 원인은 `plan: starter` — 렌더가 요금제를 사양 이름
(`0.5c-512mb`)으로 바꿨다. 옛 이름을 쓰면 "A Blueprint file was found, but there
was an issue" 로만 알려주고 무엇이 틀렸는지는 말해주지 않는다.

### PG 심사 접수 완료 (2026-08-30)

| | |
|---|---|
| 신청 결제수단 | KG이니시스(카드) · NHN KCP(카드) · 카카오페이(간편결제) |
| 가입비·연회비 | 전부 0원 |
| 통신판매업 신고 | 2026-인천부평-1357 |
| 사전 점검 | 8개 항목 전부 통과 |

카드사를 둘 신청한 것은 보험이다. 둘 다 무료이고, 신규 사업자는 반려 가능성이
실제로 있는데 한 곳이 막혀도 열흘을 날리지 않는다.
네이버페이·토스페이는 나중에 해당 수단만 추가 심사로 붙일 수 있다.

**심사 7~10 영업일. 그 동안 할 일:**

1. 상품 카탈로그 3개 → 12개 (엔진 변경 없음)
2. 화면 전면 개편 — 브랜드·용어 병기·블러 미리보기·중간 결제 버튼
3. ~~주문 저장소 Postgres 교체~~ **완료 (아래)**
4. 포트원 테스트 모드로 결제 전 과정 검증

심사가 끝나면 렌더 환경변수 4개(`PORTONE_API_SECRET`, `PORTONE_STORE_ID`,
`PORTONE_CHANNEL_KEY`, `ANTHROPIC_API_KEY`)를 채우는 것으로 결제가 열린다.
코드는 건드리지 않는다.


## 왜 결제 없이 먼저 띄우는가

순서가 이렇게 얽혀 있다.

```
사이트가 떠 있어야  →  PG 심사를 통과하고
PG 심사를 통과해야  →  결제 기능을 켤 수 있다
```

그래서 결제를 켜고 나서 배포하려 하면 영원히 못 띄운다.
**심사 대기 모드**(`apps/api/src/standby.ts`)가 이 매듭을 푼다 —
포트원 설정이 없으면 결제 버튼을 감춘 채로 뜨고, 무료 풀이와 정책 페이지는 그대로 돈다.

꺼진 결제를 조용히 성공시키지 않고 402로 거부하는 이유는, 설정 누락이
"결제는 됐는데 리포트가 안 나온다"로 나타나면 그때는 이미 돈이 오간 뒤이기 때문이다.

## 무엇을 쓰는가

| | 선택 | 이유 |
|---|---|---|
| 도메인 | 가비아 `neulbomsaju.co.kr` | 구매 완료 |
| 서버 | 렌더(Render) 싱가포르 | 웹 화면만으로 끝난다. 명령어 안 씀. 한국에서 가장 가까운 지역 |
| 이미지 | `Dockerfile` | 플랫폼에 묶이지 않는다. 나중에 옮길 일이 반드시 생긴다 |

렌더가 아니어도 된다. `Dockerfile` 은 Fly·Railway·클라우드타입 어디서나 돈다.
`render.yaml` 만 렌더 전용이다.

### 겪은 배포 사고 두 건

**1. 저장소가 사이트를 죽였다.** `startApi()` 앞에 `await migrate(pool)` 을 두어,
DB가 준비되기 전에 배포가 돌면 서버가 기동에서 멈췄다. 약관 페이지까지 같이 죽는다.
→ 결제가 꺼져 있으면 DB에 못 붙어도 뜨게 고쳤다 (`apps/api/src/main.ts`).

**2. 도커 이미지에 `pg` 가 없었다.** Dockerfile이 `packages/report` 의 의존성만
손으로 적어 설치하고 있었는데, `packages/store` 를 만들며 붙인 `pg` 를 추가하는 것을
잊었다. **빌드는 통과하고 실행할 때 죽는다** — 도커 빌드는 앱을 돌려보지 않는다.
→ 목록을 손으로 관리하는 한 또 난다. 의존성이 있는 패키지를 **찾아서** 설치하게
바꿨다. 새 패키지에 라이브러리를 붙여도 Dockerfile을 고칠 필요가 없다.

둘 다 같은 교훈이다 — **검증이 닿지 않는 곳에서 사고가 난다.**
로컬 검증 647건이 전부 통과해도 도커 이미지 안은 그 밖이었다.

> ⚠️ **이미지 빌드는 도커 데몬이 없어 직접 돌려보지 못했다.** 개발 환경에 도커 데몬이 없어
> `docker build` 를 돌려보지 못했다. 첫 배포에서 실패하면 그 로그를 보고 고쳐야 한다.
> `node apps/manse-viewer/build.mjs` 와 서버 기동 자체는 검증됐다.

## 사장님이 하실 일

### 1. 렌더 가입

`render.com` → GitHub 계정으로 가입. 카드 등록 필요 (월 $7, 약 1만원).

무료 요금제는 15분 놀면 서버가 잠든다. **심사자가 열었을 때 잠들어 있으면 반려된다.**
그래서 유료로 시작한다.

> 렌더는 요금제를 `starter` 같은 이름이 아니라 `0.5c-512mb` 처럼 **사양 이름**으로 부른다.
> 옛 이름을 쓰면 청사진이 "A Blueprint file was found, but there was an issue" 로 거부된다.

### 2. 저장소 연결

`New` → `Blueprint` → 이 저장소 선택.
`render.yaml` 을 읽고 서비스를 알아서 만든다.

### 3. 환경변수 채우기

렌더 대시보드의 `Environment` 에서 아래를 입력한다.
**사업자등록증과 글자 하나까지 같아야 한다** — 다르면 심사에서 반려된다.

| 이름 | 넣을 값 |
|---|---|
| `SITE_NAME` | 늘봄사주 |
| `SITE_URL` | https://neulbomsaju.co.kr |
| `BIZ_COMPANY` | 사업자등록증의 상호 |
| `BIZ_REPRESENTATIVE` | 대표자 성명 |
| `BIZ_REG_NUMBER` | 000-00-00000 |
| `BIZ_MAIL_ORDER_NUMBER` | 통신판매업 신고번호 (아직 없으면 비워둠) |
| `BIZ_ADDRESS` | 사업장 소재지 |
| `BIZ_PHONE` | 고객센터 전화 |
| `BIZ_EMAIL` | 고객센터 이메일 |

포트원·API 키 넷은 **나중에** 채운다. 지금 비워두면 심사 대기 모드로 뜬다.

값이 비면 화면에 `[미입력: 상호]` 가 그대로 보인다. 조용한 빈칸이 반려 사유라서
일부러 눈에 띄게 만들어 뒀다.

### 4. 도메인 연결

렌더에서 `Settings` → `Custom Domains` → `neulbomsaju.co.kr` 추가.

그러면 렌더가 **가비아에 넣을 값**을 화면에 띄운다. 그 값을 그대로 복사해서:

가비아 → `My가비아` → `서비스 관리` → 도메인 → `DNS 관리` → 레코드 추가

> 렌더가 주는 값은 계정마다 다르고 가끔 바뀐다. 여기에 적어두면 낡으므로,
> **화면에 뜬 값을 그대로** 쓴다.

DNS는 퍼지는 데 10분~1시간 걸린다. HTTPS 인증서는 렌더가 알아서 붙인다.

### 5. 확인

- `https://neulbomsaju.co.kr` — 사이트가 뜨는가
- `https://neulbomsaju.co.kr/terms` `/privacy` `/refund` — 세 페이지가 뜨는가
- 화면 맨 아래 사업자 정보에 `[미입력: ...]` 이 남아 있지 않은가

### 6. 포트원에 제출

`서비스 URL` 칸에 `https://neulbomsaju.co.kr` 넣고 신청.

## 심사 통과한 뒤

포트원에서 상점 아이디와 채널키가 나오면, 렌더 환경변수에 넷을 채우고 재배포한다.
그 순간부터 결제 버튼이 켜진다. 코드는 안 건드린다.

```
PORTONE_API_SECRET      포트원 관리자 → API 키
PORTONE_STORE_ID        포트원 관리자 → 상점 정보
PORTONE_CHANNEL_KEY     포트원 관리자 → 결제 연동 → 채널
ANTHROPIC_API_KEY       콘솔에서 새로 발급 (이전 키는 폐기됨)
```

## 주문 저장소 (2026-08-30 완료)

`packages/store` — Postgres. 진짜 DB에 대고 검증했다 (36건).

```
orders    주문·상태·결제식별자·고지여부·풀이내용(JSONB)
reports   리포트 본문. 주문 삭제 시 함께 지워진다 (CASCADE)
```

**결제가 켜져 있는데 `DATABASE_URL`이 없으면 서버가 뜨지 않는다.**
그 조합에서 재시작이 한 번 일어나면 "돈은 나갔는데 주문이 없다"가 되고,
그건 되돌릴 수 없다. 조용히 굴러가는 것보다 죽는 편이 낫다.

심사 대기 중(결제 꺼짐)에는 `DATABASE_URL` 없이도 뜬다 — 파는 것이 없으므로
주문이 사라져도 잃을 것이 없다.

검증한 것 중 핵심은 이것이다: **서버를 죽였다 다시 띄워도 산 사람이
리포트를 받는다.** HTTP로 실제 구매를 돌리고 서버를 내렸다 올려서 확인했다.

늦게 도착한 웹훅이 이미 환불된 주문을 결제됨으로 되돌리는 사고도 SQL에서 막았다
(`WHERE orders.status <> 'refunded' OR EXCLUDED.status = 'refunded'`).

보관 기간은 5년 — 전자상거래법 제6조. 개인정보처리방침에 적은 기간과 같다.
자동 삭제는 걸지 않았다 (`PURGE_SQL`만 두었다). 지우는 일은 되돌릴 수 없으므로
사람이 확인하고 실행해야 한다.

## 남은 숙제

- 도커 이미지 빌드 미검증.
- 약관·개인정보처리방침·환불정책은 법령을 읽고 쓴 것이지 변호사 검토를 받은 것이 아니다.
