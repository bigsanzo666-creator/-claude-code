# 늘봄사주 — 작업 규칙

`https://neulbomsaju.co.kr` · 렌더(싱가포르) · PG 심사 대기 중

---

## 사장님께 말하는 법 — 제일 먼저 지킬 것

**열 살 아이에게 설명한다고 생각하고 쓴다.**

- 영어 단어, 프로그램 용어, 파일 경로를 그냥 던지지 않는다.
  꼭 필요하면 바로 옆에 쉬운 말로 뜻을 붙인다.
- 표·목록·검증 결과를 잔뜩 늘어놓지 않는다. 사장님이 알아야 할 것만 몇 줄로.
- 무엇을 했는지, 이제 사장님이 뭘 하면 되는지. 이 둘만 말한다.

**왜 그렇게 됐는지는 설명하지 않는다.**

사장님이 고칠 것을 알려주면 그냥 고친다. 원인이 무엇이었는지, 어느 줄에서
그랬는지, 왜 그런 일이 생겼는지 — 묻지 않으면 한 글자도 쓰지 않는다.
그 설명은 사장님 시간을 버리는 것이다.

---

## 배포 전에 반드시 확인할 것

**여기 적힌 것은 전부 실제로 사고가 났던 항목이다.** 새로 추가할 때는
"이론적으로 위험해서"가 아니라 **터진 뒤에** 적는다.

### ① 새 패키지에 npm 라이브러리를 붙였는가

→ **`Dockerfile`이 그걸 설치하는지 확인한다.**

도커 빌드는 앱을 **돌려보지 않는다.** 그래서 라이브러리가 빠져도 빌드는 통과하고
**실행할 때 죽는다.** 로컬 검증이 전부 통과해도 이미지 안은 그 밖이다.

> 실제 사고 (2026-08-31): `packages/store` 에 `pg` 를 붙이고 Dockerfile에 안 넣어
> `Cannot find package 'pg'` 로 배포 실패. 사장님이 배포를 누른 뒤에야 드러났다.

지금은 의존성이 있는 패키지를 **찾아서** 설치하므로 목록을 고칠 일이 없다.
그 구조를 손으로 적는 방식으로 되돌리지 말 것.

### ② 기동 경로(`apps/api/src/main.ts`)를 건드렸는가

→ **외부 자원이 없거나 죽은 상태로 실제로 띄워 본다.**

```
DATABASE_URL='postgres://nobody@127.0.0.1:59999/nope' PORT=3999 \
  node --experimental-strip-types apps/api/src/main.ts
```

`/healthz` 와 `/terms` 가 200이어야 한다.

> 실제 사고 (2026-08-31): `startApi()` 앞에 `await migrate(pool)` 을 두어, DB가
> 준비되기 전에 배포가 돌면 서버가 기동에서 멈췄다. **사이트 전체가 내려간다** —
> 심사자가 봐야 할 약관 페이지까지.

**저장소·모델·PG 어느 것도 사이트를 죽여선 안 된다.** 결제가 켜져 있을 때
DB가 없는 경우만 예외다 (그건 죽는 게 맞다).

### ③ 배포한 뒤 실제로 확인한다

푸시하고 끝내지 않는다. 렌더는 새 버전이 실패하면 **옛 버전을 그대로 살려두므로,
사이트가 멀쩡해 보여도 배포는 실패했을 수 있다.**

```
curl -sS https://neulbomsaju.co.kr/healthz
curl -sS https://neulbomsaju.co.kr/products | grep -o '<h3[^>]*>[^<]*</h3>'
```

바뀐 내용이 실제로 보이는지 눈으로 대조한다.

---

## 돈과 법이 걸린 곳 — 함부로 바꾸지 말 것

- **가격은 `packages/commerce/src/catalog.ts` 한 곳에서만 온다.** 화면·미리보기·
  묶음 할인이 전부 거기서 계산된다. 다른 데 숫자를 적지 말 것.
- **정가를 지어내지 않는다.** 묶음 할인의 「따로 사면」은 구성 상품 **실제 판매가의
  합계**다. 판 적 없는 정가를 적으면 표시광고법상 거짓·과장광고다.
- **광고 수신 동의는 기본 체크하지 않는다.** 정보통신망법 제50조. 결과 전달 동의와
  광고 수신 동의는 **별개로** 받는다.
- **다크패턴을 따라 하지 않는다.** 이탈 방지 모달은 두 버튼을 같은 무게로 두고,
  거짓 긴급성("지금만")을 쓰지 않는다. 2024년 개정 전자상거래법 규제 대상이다.
- **약관·정책 문서와 코드가 다른 말을 하면 그 자체가 위반이다.** 환불 기간 같은
  숫자는 `packages/commerce/src/refund.ts` 상수를 가져다 쓰고, 검증이 일치를 본다.

## 리포트 원칙

- **판단은 룰 엔진이, 문장은 모델이.** 모델이 만세력을 계산하게 두지 않는다.
- **모든 결론에 근거를 단다.** 어느 글자·어느 부위에서 나왔는지 표시한다.
- **겁주지 않는다.** 없는 것을 "막혔다"고 쓰지 않는다.
- **용어를 던지고 끝내지 않는다.** 큰 글씨는 쉬운 말, 옆에 작은 글씨로 명리 용어와
  한 줄 뜻. 이게 경쟁사와의 차이다.

## 검증

각 패키지에서 `node --experimental-strip-types verify.ts`.
`packages/store` 와 `apps/api` 의 일부는 `DATABASE_URL` 이 있어야 전부 돈다.

**검증이 닿지 않는 곳에서 사고가 난다.** 위 배포 항목들이 전부 그랬다.
